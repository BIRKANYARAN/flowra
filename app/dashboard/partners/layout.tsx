// Server layout — sets the browser-tab title for the (client) Ortaklar hub.
import type { ReactNode } from 'react'

export const metadata = { title: 'Ortaklar' }

export default function PartnersLayout({ children }: { children: ReactNode }) {
  return children
}
