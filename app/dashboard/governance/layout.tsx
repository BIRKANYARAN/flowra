// Server layout — sets the browser-tab title for the (client) Yönetişim hub.
import type { ReactNode } from 'react'

export const metadata = { title: 'Yönetişim' }

export default function GovernanceLayout({ children }: { children: ReactNode }) {
  return children
}
